"""测试设计方法论：显式注入到生成流程，保证每条用例可追溯到方法论来源。"""

# 流水线生成与评审覆盖的核心四法。
# 注意：MockGeneratorAgent 按此键集产出用例，ReviewerAgent 逐项检查覆盖并打分，
# 增删此 dict 会直接影响 mock 满分契约（tests/test_pipeline.py），扩充方法请加到 EXTRA_METHODS。
METHODS = {
    "等价类": "将输入划分为有效/无效等价类，每类取一个代表值以缩减用例数量",
    "边界值": "在输入取值边界及其相邻点取值（min-1/min/max/max+1），边界最易出错",
    "场景法": "覆盖业务主流程的正常分支与异常分支，串成端到端场景",
    "错误猜测": "基于经验补充特殊字符、注入、空值、大小写等易漏用例",
}

# 平台层补充的设计方法：用于手工用例设计与用例库标注（不参与 mock 生成与评审覆盖检查）。
EXTRA_METHODS = {
    "判定表": "多条件组合决定输出的场景，穷举条件组合保证业务规则覆盖",
    "正交试验": "用正交表从海量参数组合中挑选代表性组合，以最少用例覆盖多因素维度",
    "状态迁移": "针对有状态的对象画出状态机，覆盖全部状态与合法/非法迁移路径",
}

# 平台全量方法（用例库筛选 / 表单下拉 / 方法论说明展示用）
ALL_METHODS = {**METHODS, **EXTRA_METHODS}

# 用例类型：标记用例验证的作用层面，与设计方法正交（一条接口用例同样可以用等价类设计）
CASE_TYPES = ("功能", "UI", "接口", "性能", "安全", "兼容")
